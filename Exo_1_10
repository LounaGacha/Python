nombre = ""

for ch in range(1,5):
    ch = input("Saisir un chiffre : " )
    while len(ch) != 1 or 48 >= ord(ch) >= 57:
        print("Ce n'est pas un chiffre !")
        ch = input("Saisir un chiffre : ")
    nombre = nombre + ch



print("Le nombre saisi est ",nombre)
